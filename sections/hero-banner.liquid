{%- liquid
  assign height_class = 'kz-hero-banner--' | append: section.settings.section_height
  assign layout_style_class = 'kz-hero-banner--layout-' | append: section.settings.layout_style | default: 'kz-hero-banner--layout-card'
  assign mobile_container_class = ''
  if section.settings.show_container_mobile
    assign mobile_container_class = 'kz-hero-banner--content-below-mobile'
  endif
  assign align_mobile_class = 'kz-hero-banner__content--align-mobile-' | append: section.settings.content_alignment_mobile | default: 'left'
  assign has_blocks = false
  if section.blocks.size > 0
    assign has_blocks = true
  endif
-%}

<hero-slider
  id="HeroBanner-{{ section.id }}"
  class="kz-hero-banner {{ height_class }} {{ layout_style_class }} {{ mobile_container_class }} {{ align_mobile_class }}"
  style="--kz-banner-radius: {{ section.settings.card_border_radius | default: 16 }}px;"
  data-auto-rotate="{{ section.settings.auto_rotate }}"
  data-speed="{{ section.settings.change_slides_every }}"
>
  <div class="kz-hero-banner__slides" data-slides-container>
    {%- if has_blocks -%}
      {%- for block in section.blocks -%}
        {%- case block.type -%}
          {%- when 'slide' -%}
            {%- liquid
              assign hero_img = block.settings.image
              assign hero_img_mobile = block.settings.image_mobile | default: hero_img
              assign align_class = 'kz-hero-banner__content--align-' | append: block.settings.content_alignment
              assign overlay_op = block.settings.overlay_opacity | default: section.settings.overlay_opacity
            -%}
            <div
              class="kz-hero-banner__slide {% if forloop.first %}is-active{% endif %}"
              data-slide-index="{{ forloop.index0 }}"
              style="--kz-overlay-opacity: {{ overlay_op | divided_by: 100.0 }};"
              {{ block.shopify_attributes }}
            >
              <div class="kz-hero-banner__media">
                {%- if hero_img != blank -%}
                  <picture>
                    {%- if hero_img_mobile != blank -%}
                      <source media="(max-width: 767px)" srcset="{{ hero_img_mobile | image_url: width: 800 }}">
                    {%- endif -%}
                    {{
                      hero_img
                      | image_url: width: 2400
                      | image_tag:
                        class: 'kz-hero-banner__image',
                        loading: 'eager',
                        fetchpriority: 'high',
                        alt: block.settings.heading
                    }}
                  </picture>
                {%- else -%}
                  <div class="kz-hero-banner__placeholder">
                    {{ 'lifestyle-1' | placeholder_svg_tag: 'kz-image-placeholder' }}
                  </div>
                {%- endif -%}
                <div class="kz-hero-banner__overlay"></div>
              </div>

              <div class="kz-hero-banner__container page-width">
                <div class="kz-hero-banner__content {{ align_class }} kz-animate-fade-up">
                  {%- if block.settings.subheading != blank -%}
                    <span class="kz-hero-banner__subheading">{{ block.settings.subheading }}</span>
                  {%- endif -%}

                  {%- if block.settings.heading != blank -%}
                    <h1 class="kz-hero-banner__heading">{{ block.settings.heading }}</h1>
                  {%- endif -%}

                  {%- if block.settings.text != blank -%}
                    <div class="kz-hero-banner__text">{{ block.settings.text }}</div>
                  {%- endif -%}

                  {%- if block.settings.button_label_1 != blank or block.settings.button_label_2 != blank -%}
                    {%- liquid
                      assign b1_variant = block.settings.button_1_style | default: 'primary'
                      assign b1_size = block.settings.button_1_size | default: 'md'
                      assign b2_variant = block.settings.button_2_style | default: 'outline'
                      assign b2_size = block.settings.button_2_size | default: 'md'
                      assign mobile_layout = section.settings.buttons_layout_mobile | default: 'inline'
                    -%}
                    <div class="kz-hero-banner__buttons kz-hero-banner__buttons--mobile-{{ mobile_layout }}">
                      {%- if block.settings.button_label_1 != blank -%}
                        {% render 'button',
                          label: block.settings.button_label_1,
                          url: block.settings.button_link_1,
                          variant: b1_variant,
                          size: b1_size
                        %}
                      {%- endif -%}

                      {%- if block.settings.button_label_2 != blank -%}
                        {%- assign hide_b2 = block.settings.hide_button_2_mobile -%}
                        <div class="{% if hide_b2 %}kz-hero-banner__btn-2-mobile-hide{% endif %}">
                          {% render 'button',
                            label: block.settings.button_label_2,
                            url: block.settings.button_link_2,
                            variant: b2_variant,
                            size: b2_size
                          %}
                        </div>
                      {%- endif -%}
                    </div>
                  {%- endif -%}
                </div>
              </div>
            </div>
        {%- endcase -%}
      {%- endfor -%}
    {%- else -%}
      {%- liquid
        assign hero_img = section.settings.image
        assign hero_img_mobile = section.settings.image_mobile | default: hero_img
        assign align_class = 'kz-hero-banner__content--align-' | append: section.settings.content_alignment
      -%}
      <div
        class="kz-hero-banner__slide is-active"
        data-slide-index="0"
        style="--kz-overlay-opacity: {{ section.settings.overlay_opacity | divided_by: 100.0 }};"
      >
        <div class="kz-hero-banner__media">
          {%- if hero_img != blank -%}
            <picture>
              {%- if hero_img_mobile != blank -%}
                <source media="(max-width: 767px)" srcset="{{ hero_img_mobile | image_url: width: 800 }}">
              {%- endif -%}
              {{
                hero_img
                | image_url: width: 2400
                | image_tag:
                  class: 'kz-hero-banner__image',
                  loading: 'eager',
                  fetchpriority: 'high',
                  alt: section.settings.heading
              }}
            </picture>
          {%- else -%}
            <div class="kz-hero-banner__placeholder">
              {{ 'lifestyle-1' | placeholder_svg_tag: 'kz-image-placeholder' }}
            </div>
          {%- endif -%}
          <div class="kz-hero-banner__overlay"></div>
        </div>

        <div class="kz-hero-banner__container page-width">
          <div class="kz-hero-banner__content {{ align_class }} kz-animate-fade-up">
            {%- if section.settings.subheading != blank -%}
              <span class="kz-hero-banner__subheading">{{ section.settings.subheading }}</span>
            {%- endif -%}

            {%- if section.settings.heading != blank -%}
              <h1 class="kz-hero-banner__heading">{{ section.settings.heading }}</h1>
            {%- endif -%}

            {%- if section.settings.text != blank -%}
              <div class="kz-hero-banner__text">{{ section.settings.text }}</div>
            {%- endif -%}

            {%- if section.settings.button_label_1 != blank or section.settings.button_label_2 != blank -%}
              <div class="kz-hero-banner__buttons kz-hero-banner__buttons--mobile-{{ section.settings.buttons_layout_mobile | default: 'inline' }}">
                {%- if section.settings.button_label_1 != blank -%}
                  {% render 'button',
                    label: section.settings.button_label_1,
                    url: section.settings.button_link_1,
                    variant: 'primary',
                    size: 'md'
                  %}
                {%- endif -%}

                {%- if section.settings.button_label_2 != blank -%}
                  <div class="kz-hero-banner__btn-2-mobile-hide">
                    {% render 'button',
                      label: section.settings.button_label_2,
                      url: section.settings.button_link_2,
                      variant: 'outline',
                      size: 'md'
                    %}
                  </div>
                {%- endif -%}
              </div>
            {%- endif -%}
          </div>
        </div>
      </div>
    {%- endif -%}
  </div>

  {%- if section.blocks.size > 1 -%}
    <div class="kz-hero-banner__controls">
      <button
        type="button"
        class="kz-hero-banner__nav-btn kz-hero-banner__nav-btn--prev"
        data-nav-prev
        aria-label="{{ 'general.pagination.previous' | t | default: 'Previous slide' }}"
      >
        {% render 'icon', icon: 'chevron-left', class: 'kz-hero-banner__nav-icon' %}
      </button>

      <div class="kz-hero-banner__dots" data-dots-container>
        {%- for block in section.blocks -%}
          <button
            type="button"
            class="kz-hero-banner__dot {% if forloop.first %}is-active{% endif %}"
            data-dot-index="{{ forloop.index0 }}"
            aria-label="Go to slide {{ forloop.index }}"
          ></button>
        {%- endfor -%}
      </div>

      <button
        type="button"
        class="kz-hero-banner__nav-btn kz-hero-banner__nav-btn--next"
        data-nav-next
        aria-label="{{ 'general.pagination.next' | t | default: 'Next slide' }}"
      >
        {% render 'icon', icon: 'chevron-right', class: 'kz-hero-banner__nav-icon' %}
      </button>
    </div>
  {%- endif -%}
</hero-slider>

{% stylesheet %}
  /* ==========================================================================
     1. HERO BANNER - BASE & SHARED STYLES
     ========================================================================== */
  .kz-hero-banner {
    position: relative;
    width: 100%;
    overflow: hidden;
    display: block;
    background: transparent;
  }

  /* Default Desktop / Tablet: Full Width Layout (Ignore Card Settings) */
  .kz-hero-banner--layout-full,
  .kz-hero-banner--layout-card {
    width: 100%;
    max-width: none;
    margin: 0;
    border-radius: 0;
    box-shadow: none;
  }

  .kz-hero-banner__slides {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .kz-hero-banner__slide {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: opacity 600ms cubic-bezier(0.4, 0, 0.2, 1), visibility 600ms cubic-bezier(0.4, 0, 0.2, 1);
    z-index: 1;
  }

  .kz-hero-banner__slide.is-active {
    opacity: 1;
    visibility: visible;
    pointer-events: auto;
    z-index: 2;
  }

  .kz-hero-banner__media {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
    overflow: hidden;
  }

  .kz-hero-banner__media picture {
    display: block;
    width: 100%;
    height: 100%;
    position: absolute;
    inset: 0;
  }

  .kz-hero-banner__image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .kz-hero-banner__placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #2b2b2b;
  }

  .kz-hero-banner__overlay {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, var(--kz-overlay-opacity, 0.3));
    z-index: 2;
  }

  .kz-hero-banner__container {
    position: relative;
    z-index: 3;
    width: 100%;
    height: 100%;
    max-width: var(--kz-page-max-width, 1400px);
    margin: 0 auto;
    display: flex;
  }

  .kz-hero-banner__content {
    display: flex;
    flex-direction: column;
    color: #ffffff;
  }

  .kz-hero-banner__subheading {
    font-family: var(--kz-font-family-body);
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: var(--kz-letter-spacing-uppercase, 0.15em);
    color: var(--kz-color-secondary, #d4a373);
  }

  .kz-hero-banner__heading {
    font-family: var(--kz-font-family-heading);
    font-weight: 700;
    color: #ffffff;
  }

  .kz-hero-banner__text {
    font-family: var(--kz-font-family-body);
    color: rgba(255, 255, 255, 0.9);
  }

  .kz-hero-banner__buttons {
    display: flex;
    align-items: center;
  }

  .kz-hero-banner .kz-button--outline {
    border-color: #ffffff;
    color: #ffffff;
  }

  .kz-hero-banner .kz-button--outline:hover {
    background-color: #ffffff;
    color: #111111;
  }

  /* Shared Slider Navigation Controls */
  .kz-hero-banner__controls {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    z-index: 10;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background-color: rgba(0, 0, 0, 0.55);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.15);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.25);
    height: auto;
    width: auto;
    box-sizing: border-box;
  }

  .kz-hero-banner__nav-btn {
    background: transparent !important;
    border: none !important;
    color: #ffffff !important;
    cursor: pointer !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    padding: 2px !important;
    margin: 0 !important;
    width: 20px !important;
    height: 20px !important;
    opacity: 0.85 !important;
    box-shadow: none !important;
    transition: opacity 150ms ease, transform 150ms ease !important;
  }

  .kz-hero-banner__nav-btn:hover {
    opacity: 1 !important;
    transform: scale(1.1) !important;
  }

  .kz-hero-banner__nav-icon {
    width: 16px !important;
    height: 16px !important;
  }

  .kz-hero-banner__dots {
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    gap: 8px !important;
    height: 12px !important;
  }

  .kz-hero-banner__dot {
    width: 8px !important;
    height: 8px !important;
    border-radius: 9999px !important;
    background-color: rgba(255, 255, 255, 0.45) !important;
    border: none !important;
    cursor: pointer !important;
    padding: 0 !important;
    margin: 0 !important;
    outline: none !important;
    flex-shrink: 0 !important;
    transition: all 250ms ease !important;
  }

  .kz-hero-banner__dot.is-active {
    width: 22px !important;
    height: 8px !important;
    background-color: #ffffff !important;
  }

  /* ==========================================================================
     2. DESKTOP LAYOUT (≥ 1024px)
     ========================================================================== */
  @media (min-width: 1024px) {
    .kz-hero-banner--small {
      height: 450px;
    }
    .kz-hero-banner--medium {
      height: 600px;
    }
    .kz-hero-banner--large {
      height: 750px;
    }
    .kz-hero-banner--full {
      height: 90vh;
      min-height: 650px;
    }

    .kz-hero-banner__container {
      padding: 48px 24px 110px 24px;
      align-items: center;
    }

    .kz-hero-banner__content {
      max-width: 640px;
    }

    .kz-hero-banner__content--align-left {
      text-align: left;
      align-items: flex-start;
    }
    .kz-hero-banner__content--align-center {
      text-align: center;
      align-items: center;
      margin: 0 auto;
    }
    .kz-hero-banner__content--align-right {
      text-align: right;
      align-items: flex-end;
      margin-left: auto;
    }

    .kz-hero-banner__subheading {
      font-size: 14px;
      margin-bottom: 12px;
    }

    .kz-hero-banner__heading {
      font-size: 64px;
      line-height: 1.1;
      margin: 0 0 16px 0;
    }

    .kz-hero-banner__text {
      font-size: 16px;
      line-height: 1.6;
      margin-bottom: 24px;
    }

    .kz-hero-banner__buttons {
      flex-direction: row;
      flex-wrap: wrap;
      gap: 16px;
    }

    .kz-hero-banner__controls {
      bottom: 72px;
      padding: 8px 16px;
      gap: 14px;
    }
  }

  /* ==========================================================================
     3. TABLET LAYOUT (768px – 1023px)
     ========================================================================== */
  @media (min-width: 768px) and (max-width: 1023px) {
    .kz-hero-banner--small {
      height: 380px;
    }
    .kz-hero-banner--medium {
      height: 480px;
    }
    .kz-hero-banner--large {
      height: 560px;
    }
    .kz-hero-banner--full {
      height: 75vh;
      min-height: 480px;
    }

    .kz-hero-banner__container {
      padding: 36px 20px 80px 20px;
      align-items: center;
    }

    .kz-hero-banner__content {
      max-width: 540px;
    }

    .kz-hero-banner__content--align-left {
      text-align: left;
      align-items: flex-start;
    }
    .kz-hero-banner__content--align-center {
      text-align: center;
      align-items: center;
      margin: 0 auto;
    }
    .kz-hero-banner__content--align-right {
      text-align: right;
      align-items: flex-end;
      margin-left: auto;
    }

    .kz-hero-banner__subheading {
      font-size: 13px;
      margin-bottom: 10px;
    }

    .kz-hero-banner__heading {
      font-size: 42px;
      line-height: 1.12;
      margin: 0 0 14px 0;
    }

    .kz-hero-banner__text {
      font-size: 15px;
      line-height: 1.5;
      margin-bottom: 20px;
    }

    .kz-hero-banner__buttons {
      flex-direction: row;
      flex-wrap: wrap;
      gap: 12px;
    }

    .kz-hero-banner__controls {
      bottom: 42px;
      padding: 6px 14px;
      gap: 12px;
    }
  }

  /* ==========================================================================
     4. MOBILE LAYOUT (320px – 767px)
     ========================================================================== */
  @media (max-width: 767px) {
    /* Banner Layout Style & Card Corner Radius Settings apply ONLY on Mobile */
    .kz-hero-banner--layout-card {
      margin: 10px 12px;
      width: calc(100% - 24px);
      border-radius: var(--kz-banner-radius, 16px);
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
      overflow: hidden;
    }

    .kz-hero-banner--layout-full {
      margin: 0;
      width: 100%;
      border-radius: 0;
      box-shadow: none;
    }

    .kz-hero-banner--small {
      height: 360px;
    }
    .kz-hero-banner--medium {
      height: 420px;
    }
    .kz-hero-banner--large {
      height: 480px;
    }
    .kz-hero-banner--full {
      height: 70vh;
      min-height: 420px;
    }

    .kz-hero-banner__container {
      padding: 24px 16px 64px 16px;
      align-items: center;
    }

    .kz-hero-banner__content {
      width: 100%;
      max-width: 100%;
    }

    .kz-hero-banner__content--align-mobile-left {
      text-align: left;
      align-items: flex-start;
    }
    .kz-hero-banner__content--align-mobile-center {
      text-align: center;
      align-items: center;
      margin: 0 auto;
    }
    .kz-hero-banner__content--align-mobile-right {
      text-align: right;
      align-items: flex-end;
      margin-left: auto;
    }

    .kz-hero-banner__subheading {
      font-size: 11px;
      letter-spacing: 0.12em;
      margin-bottom: 6px;
    }

    .kz-hero-banner__heading {
      font-size: clamp(22px, 6vw, 32px);
      line-height: 1.15;
      margin: 0 0 8px 0;
    }

    .kz-hero-banner__text {
      font-size: 13px;
      line-height: 1.4;
      margin-bottom: 16px;
    }

    /* Mobile Compact Luxury Pill Buttons */
    .kz-hero-banner__buttons {
      width: 100%;
      display: flex;
      flex-direction: row;
      flex-wrap: wrap;
      gap: 10px;
    }

    .kz-hero-banner__content--align-mobile-center .kz-hero-banner__buttons {
      justify-content: center;
    }

    .kz-hero-banner__content--align-mobile-left .kz-hero-banner__buttons {
      justify-content: flex-start;
    }

    .kz-hero-banner__content--align-mobile-right .kz-hero-banner__buttons {
      justify-content: flex-end;
    }

    .kz-hero-banner__buttons .kz-button,
    .kz-hero-banner__buttons a,
    .kz-hero-banner__buttons button {
      width: fit-content !important;
      flex: 0 0 auto !important;
      height: 42px !important;
      min-height: 42px !important;
      padding: 0 28px !important;
      font-size: 13px !important;
      border-radius: 999px !important;
      display: inline-flex !important;
      align-items: center !important;
      justify-content: center !important;
      box-sizing: border-box !important;
      letter-spacing: 0.05em !important;
      white-space: nowrap !important;
    }

    .kz-hero-banner__btn-2-mobile-hide {
      display: none !important;
    }

    .kz-hero-banner__controls {
      bottom: 18px;
      padding: 4px 10px;
      gap: 8px;
    }

    /* Content Below Mobile Option */
    .kz-hero-banner--content-below-mobile {
      height: auto !important;
    }

    .kz-hero-banner--content-below-mobile .kz-hero-banner__slides {
      min-height: 0;
      height: auto;
    }

    .kz-hero-banner--content-below-mobile .kz-hero-banner__slide {
      position: absolute;
      inset: 0;
      flex-direction: column;
      height: auto;
    }

    .kz-hero-banner--content-below-mobile .kz-hero-banner__slide.is-active {
      position: relative;
    }

    .kz-hero-banner--content-below-mobile .kz-hero-banner__media {
      position: relative;
      height: 240px;
      width: 100%;
    }

    .kz-hero-banner--content-below-mobile .kz-hero-banner__container {
      position: relative;
      background-color: var(--kz-color-surface-dark, #161616);
      padding: 20px 16px 64px 16px;
      width: 100%;
      height: auto;
      align-items: flex-start;
    }
  }
{% endstylesheet %}

{% javascript %}
  if (!customElements.get('hero-slider')) {
    customElements.define(
      'hero-slider',
      class HeroSlider extends HTMLElement {
        constructor() {
          super();
          this.slides = this.querySelectorAll('.kz-hero-banner__slide');
          this.dots = this.querySelectorAll('.kz-hero-banner__dot');
          this.prevBtn = this.querySelector('[data-nav-prev]');
          this.nextBtn = this.querySelector('[data-nav-next]');
          this.autoRotate = this.dataset.autoRotate === 'true';
          this.speed = (parseInt(this.dataset.speed, 10) || 5) * 1000;
          this.currentIndex = 0;
          this.timer = null;

          if (this.slides.length <= 1) return;

          if (this.prevBtn) this.prevBtn.addEventListener('click', () => this.prev());
          if (this.nextBtn) this.nextBtn.addEventListener('click', () => this.next());

          this.dots.forEach((dot) => {
            dot.addEventListener('click', () => {
              const index = parseInt(dot.dataset.dotIndex, 10);
              this.goToSlide(index);
            });
          });

          if (this.autoRotate) {
            this.startAutoRotate();
            this.addEventListener('mouseenter', () => this.stopAutoRotate());
            this.addEventListener('mouseleave', () => this.startAutoRotate());
          }
        }

        goToSlide(index) {
          this.slides[this.currentIndex].classList.remove('is-active');
          if (this.dots[this.currentIndex]) {
            this.dots[this.currentIndex].classList.remove('is-active');
          }

          this.currentIndex = index;

          this.slides[this.currentIndex].classList.add('is-active');
          if (this.dots[this.currentIndex]) {
            this.dots[this.currentIndex].classList.add('is-active');
          }
        }

        next() {
          const nextIndex = (this.currentIndex + 1) % this.slides.length;
          this.goToSlide(nextIndex);
        }

        prev() {
          const prevIndex = (this.currentIndex - 1 + this.slides.length) % this.slides.length;
          this.goToSlide(prevIndex);
        }

        startAutoRotate() {
          if (this.timer) clearInterval(this.timer);
          this.timer = setInterval(() => this.next(), this.speed);
        }

        stopAutoRotate() {
          if (this.timer) clearInterval(this.timer);
        }
      }
    );
  }
{% endjavascript %}

{% schema %}
{
  "name": "Hero Slider",
  "tag": "section",
  "class": "section-hero-banner",
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Fallback hero image"
    },
    {
      "type": "image_picker",
      "id": "image_mobile",
      "label": "Fallback mobile image"
    },
    {
      "type": "select",
      "id": "section_height",
      "label": "Section height",
      "options": [
        { "value": "small", "label": "Small (450px)" },
        { "value": "medium", "label": "Medium (600px)" },
        { "value": "large", "label": "Large (750px)" },
        { "value": "full", "label": "Full Screen (90vh)" }
      ],
      "default": "medium"
    },
    {
      "type": "range",
      "id": "overlay_opacity",
      "min": 0,
      "max": 90,
      "step": 5,
      "unit": "%",
      "label": "Overlay opacity",
      "default": 30
    },
    {
      "type": "checkbox",
      "id": "auto_rotate",
      "label": "Auto-rotate slides",
      "default": true
    },
    {
      "type": "range",
      "id": "change_slides_every",
      "min": 3,
      "max": 10,
      "step": 1,
      "unit": "s",
      "label": "Change slides every",
      "default": 5
    },
    {
      "type": "text",
      "id": "subheading",
      "default": "Autumn / Winter Collection",
      "label": "Default subheading"
    },
    {
      "type": "text",
      "id": "heading",
      "default": "Timeless Elegance & Modern Aesthetics",
      "label": "Default heading"
    },
    {
      "type": "richtext",
      "id": "text",
      "default": "<p>Discover curated luxury fashion pieces designed to elevate your everyday capsule wardrobe.</p>",
      "label": "Default body text"
    },
    {
      "type": "select",
      "id": "content_alignment",
      "label": "Content alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "center"
    },
    {
      "type": "text",
      "id": "button_label_1",
      "default": "Shop New Arrivals",
      "label": "Default primary button label"
    },
    {
      "type": "url",
      "id": "button_link_1",
      "label": "Default primary button link"
    },
    {
      "type": "text",
      "id": "button_label_2",
      "default": "Explore Lookbook",
      "label": "Default secondary button label"
    },
    {
      "type": "url",
      "id": "button_link_2",
      "label": "Default secondary button link"
    },
    {
      "type": "header",
      "content": "Card & Container Style"
    },
    {
      "type": "select",
      "id": "layout_style",
      "label": "Banner layout style",
      "options": [
        { "value": "full", "label": "Full width" },
        { "value": "card", "label": "Rounded Card (Container)" }
      ],
      "default": "card"
    },
    {
      "type": "range",
      "id": "card_border_radius",
      "min": 0,
      "max": 32,
      "step": 2,
      "unit": "px",
      "label": "Card corner radius",
      "default": 16
    },
    {
      "type": "header",
      "content": "Mobile Settings"
    },
    {
      "type": "checkbox",
      "id": "show_container_mobile",
      "label": "Show content below image on mobile",
      "default": false
    },
    {
      "type": "select",
      "id": "content_alignment_mobile",
      "label": "Mobile content alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "left"
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Hero Slide",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Slide image"
        },
        {
          "type": "image_picker",
          "id": "image_mobile",
          "label": "Mobile slide image"
        },
        {
          "type": "range",
          "id": "overlay_opacity",
          "min": 0,
          "max": 90,
          "step": 5,
          "unit": "%",
          "label": "Slide overlay opacity",
          "default": 30
        },
        {
          "type": "text",
          "id": "subheading",
          "default": "New Arrivals",
          "label": "Subheading"
        },
        {
          "type": "text",
          "id": "heading",
          "default": "Elevate Your Style",
          "label": "Heading"
        },
        {
          "type": "richtext",
          "id": "text",
          "default": "<p>Explore our latest seasonal arrivals crafted with premium fabrics.</p>",
          "label": "Body text"
        },
        {
          "type": "select",
          "id": "content_alignment",
          "label": "Content alignment",
          "options": [
            { "value": "left", "label": "Left" },
            { "value": "center", "label": "Center" },
            { "value": "right", "label": "Right" }
          ],
          "default": "center"
        },
        {
          "type": "text",
          "id": "button_label_1",
          "default": "Shop Collection",
          "label": "Primary button label"
        },
        {
          "type": "url",
          "id": "button_link_1",
          "label": "Primary button link"
        },
        {
          "type": "select",
          "id": "button_1_style",
          "label": "Primary button style",
          "options": [
            { "value": "primary", "label": "Solid Primary" },
            { "value": "outline", "label": "Outline" },
            { "value": "pill", "label": "Rounded Pill" }
          ],
          "default": "primary"
        },
        {
          "type": "select",
          "id": "button_1_size",
          "label": "Primary button size",
          "options": [
            { "value": "sm", "label": "Small" },
            { "value": "md", "label": "Medium" },
            { "value": "lg", "label": "Large" }
          ],
          "default": "md"
        },
        {
          "type": "text",
          "id": "button_label_2",
          "default": "View Details",
          "label": "Secondary button label"
        },
        {
          "type": "url",
          "id": "button_link_2",
          "label": "Secondary button link"
        },
        {
          "type": "select",
          "id": "button_2_style",
          "label": "Secondary button style",
          "options": [
            { "value": "primary", "label": "Solid Primary" },
            { "value": "outline", "label": "Outline" },
            { "value": "pill", "label": "Rounded Pill" }
          ],
          "default": "outline"
        },
        {
          "type": "select",
          "id": "button_2_size",
          "label": "Secondary button size",
          "options": [
            { "value": "sm", "label": "Small" },
            { "value": "md", "label": "Medium" },
            { "value": "lg", "label": "Large" }
          ],
          "default": "md"
        },
        {
          "type": "checkbox",
          "id": "hide_button_2_mobile",
          "label": "Hide Secondary button on mobile",
          "default": true
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Hero Slider",
      "blocks": [
        {
          "type": "slide"
        }
      ]
    }
  ]
}
{% endschema %}
