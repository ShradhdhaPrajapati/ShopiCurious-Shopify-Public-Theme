# Exclude configuration data files from Shopify CLI operations
# This prevents Theme Editor customizations (uploaded images, colors, typography, settings)
# from being overwritten when running `shopify theme push`.

config/settings_data.json
config/settings_data.*.json
